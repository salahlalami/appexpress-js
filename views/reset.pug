extends layoutLogin

//- Reset page 

block loginPage
    .background
    .container
        .col-24   
            .loginBox
                != h.icon('logo-cedimed')
                form.form(method="POST")
                  input(type="password" name="password" placeholder="Password")
                  input(type="password" name="password-confirm" placeholder="Confirm Password")
                  button(type="submit" class="save")  Reset Password
